<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Lecture audio NFC</title>
  <style>
    body {
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      margin: 0;
      display: flex;
      min-height: 100vh;
      align-items: center;
      justify-content: center;
      background: #f5f5f5;
      color: #111;
    }
    .card {
      max-width: 600px;
      padding: 24px;
      border-radius: 16px;
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
      background: white;
      text-align: center;
    }
    h1 {
      font-size: 22px;
      margin: 0 0 10px;
    }
    p {
      margin: 0 0 16px;
      color: #444;
    }
    audio {
      width: 100%;
      margin-top: 12px;
    }
    #playButton {
      display: none;
      background-color: #007bff;
      color: white;
      border: none;
      padding: 12px 18px;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    #playButton:hover {
      background-color: #0056b3;
    }
    small {
      display: block;
      margin-top: 12px;
      color: #777;
    }
  </style>
</head>
<body>
  <div class="card">
    <h1>🎵 Lecture audio NFC</h1>
    <p id="status">Tentative de lecture automatique du fichier audio...</p>

    <!-- 🔊 Remplace le lien ci-dessous par ton fichier MP3 -->
    <audio id="player" controls playsinline preload="auto"
      src="src="https://www.dropbox.com/scl/fi/n31tms5eybaespxurba7p/Antonio-Vivaldi-Allegro-non-molto-from-Winter-from-The-Four-Seasons.mp3?rlkey=iebe0o5wgdo2dg3p3q52lxnpc&st=pztzyuf9&dl=1""></audio>

    <button id="playButton">▶️ Appuyer pour lancer l’audio</button>
    <small>Si la lecture automatique est bloquée, appuyez sur le bouton.</small>
  </div>

  <script>
    const player = document.getElementById('player');
    const playBtn = document.getElementById('playButton');
    const status = document.getElementById('status');

    async function tryPlay() {
      try {
        await player.play();
        status.textContent = 'Lecture en cours — profitez du son 🎶';
        playBtn.style.display = 'none';
      } catch (err) {
        status.textContent = 'Lecture automatique bloquée. Appuyez sur le bouton pour lancer le son.';
        playBtn.style.display = 'inline-block';
      }
    }

    // Lancer automatiquement à l’ouverture de la page
    window.addEventListener('load', tryPlay);

    // Si autoplay est bloqué, lecture manuelle
    playBtn.addEventListener('click', async () => {
      try {
        await player.play();
        status.textContent = 'Lecture en cours — merci !';
        playBtn.style.display = 'none';
      } catch (e) {
        status.textContent = 'Erreur lors du démarrage : ' + (e.message || e);
      }
    });
  </script>
</body>
</html>
